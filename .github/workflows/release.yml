name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install playwright pyinstaller
          python -m playwright install chromium

      - name: Build executable
        run: python build.py

      - name: Bundle as .app
        run: |
          export APP="dist/JH Assistant.app"
          mkdir -p "$APP/Contents/MacOS"
          cp dist/jh_assistant "$APP/Contents/MacOS/jh_assistant"
          chmod +x "$APP/Contents/MacOS/jh_assistant"
          python3 -c "
          import os, plistlib
          ref = os.environ['GITHUB_REF_NAME']
          app = os.environ['APP']
          plistlib.dump({
              'CFBundleExecutable': 'jh_assistant',
              'CFBundleIdentifier': 'com.jobhunt.assistant',
              'CFBundleInfoDictionaryVersion': '6.0',
              'CFBundleName': 'JH Assistant',
              'CFBundlePackageType': 'APPL',
              'CFBundleShortVersionString': ref,
              'CFBundleVersion': ref,
              'LSUIElement': True,
              'NSHighResolutionCapable': True,
          }, open(os.path.join(app, 'Contents', 'Info.plist'), 'wb'))
          "
          codesign --force --deep --sign - "$APP"

      - name: Zip .app
        run: |
          cd dist && zip -r jh-assistant-macos.zip "JH Assistant.app"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: jh-assistant-macos
          path: dist/jh-assistant-macos.zip

      - name: Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: dist/jh-assistant-macos.zip
